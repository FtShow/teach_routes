import React from 'react';

export const Login = () => {
    return (
        <div>
            <input type="text" placeholder={'login'}/>
            <input type="text" placeholder={'password'}/>

        </div>
    );
};
